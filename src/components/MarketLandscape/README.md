# MarketLandscape 组件

## 作用
承接上文痛点，用具体的数据和“传统巨头设备”的客观局限性来论证市场为什么急需破局者。展现对现有竞品的深刻理解。

## 动画与交互设计
1. **横向滑入动画**：使用 `x: -50` 到 `x: 0` 的平移效果，让各个传统设备的卡片从屏幕左侧带弹簧效果 (`spring`) 划入，有种“清单逐个列出”的压迫感。
2. **UI 布局**：采用横向列表（ListView）而非网格，强调对比感。不同维度（设备名、价格、局限性）一目了然。
